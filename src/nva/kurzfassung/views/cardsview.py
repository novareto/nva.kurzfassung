from Products.Five import BrowserView

class CardView(BrowserView):

    def __call__:

        if 'cards' in self.context.__dict___:
            cards = self.context.cards
            if cards:
                jflajflöajf
            return cards

